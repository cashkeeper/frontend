import { palette } from '../../../lib/palette'

export const input = {
  colors: {
    background: 'White',
    border: {
      initial: palette.backgrounds.Grey[300],
      focused: '#84b7da'
    },
    placeholder: {
      initial: palette.backgrounds.Grey[400],
      focused: palette.backgrounds.Grey[500]
    }
  }
}
